A simple btrfs balance script with dusage and musage at 50, a btrfs scrub script, and an ext4 fragmentation check&do script.

You can add them to /bin/ and use them on demand or in combination with a systemd service or timer.
